# quickstart-app

A minimal Express app scaffolded as a standalone project.

## Run locally

```bash
npm install
npm start
```

Then open <http://localhost:3000>.

## Initialize as a separate Git repository

```bash
git init
git add .
git commit -m "Initial commit"
```
